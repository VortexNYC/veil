openapi: 3.1.0
info:
  description: "Agent credential broker. Use injects the secret at the edge. Responses\
    \ never include vault material. CLI, MCP, and generated SDKs are projections of\
    \ this contract."
  title: Veil
  version: 0.1.1
servers:
- url: https://veil.nyc
security:
- bearerAuth: []
tags:
- description: Bearer is the agent. listItems is metadata. useItem injects. Never
    GetSecret.
  name: Agent
- description: "Bearer is a Keto member human. Create items and grants. Secret may\
    \ be sent, never returned."
  name: Owner
- description: Bearer is a verified human ID token. Pre-membership — provision creates
    the membership.
  name: Human
paths:
  /health:
    get:
      operationId: getHealth
      responses:
        "200":
          content:
            text/plain:
              schema:
                type: string
          description: ok
      security: []
      summary: Liveness. No secrets.
      tags:
      - Agent
  /openapi.json:
    get:
      operationId: getOpenApi
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
          description: OpenAPI document
      security: []
      summary: This contract.
      tags:
      - Agent
  /v1/items:
    get:
      operationId: listItems
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemsResponse"
          description: Items
        "401":
          description: missing or invalid Bearer
      summary: "Items this principal may see. Agent: granted. Human owner: the org.\
        \ Human member: granted. Names and URIs. Never secrets."
      tags:
      - Agent
    post:
      operationId: createItem
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateItemRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"
          description: Item metadata. No secret.
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: agent cannot create items
      summary: Create an item. Secret is in the request over TLS. Never in the response.
        Not MCP.
      tags:
      - Owner
  /v1/import:
    post:
      operationId: importItems
      parameters:
      - description: export.1pux or chrome.csv. Sniffed from bytes when empty.
        explode: true
        in: query
        name: filename
        required: false
        schema:
          type: string
        style: form
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
          text/csv:
            schema:
              format: binary
              type: string
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResponse"
          description: Created item names. No secrets.
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: agent cannot import
      summary: "One-shot 1Password .1pux or CSV onto origin. Secret in the file, never\
        \ in the response. Not MCP."
      tags:
      - Owner
  /v1/items/{name}:
    delete:
      operationId: deleteItem
      parameters:
      - explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          description: deleted
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: agent cannot delete items
      summary: Remove the item and its grants. Not MCP.
      tags:
      - Owner
    patch:
      operationId: updateItem
      parameters:
      - explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateItemRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"
          description: Item metadata
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: agent cannot update items
      summary: "Replace URIs, tags, and fill username. No secret."
      tags:
      - Owner
  /v1/items/{name}/archive:
    post:
      operationId: archiveItem
      parameters:
      - explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          description: archived
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: agent cannot archive items
      summary: Hide from Use and list. History stays.
      tags:
      - Owner
  /v1/grants:
    get:
      operationId: listGrants
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrantsResponse"
          description: Grants
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Grants in this org. No secrets. Not MCP.
      tags:
      - Owner
    post:
      operationId: createGrant
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGrantRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Grant"
          description: Grant
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Grant an agent or a Kratos human Use on an item. Same grant object.
        Not MCP. Not a family vault.
      tags:
      - Owner
  /v1/agents:
    get:
      operationId: listAgents
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentsResponse"
          description: Agents
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Agents in this org. Ids only. Never secrets. Not MCP.
      tags:
      - Owner
    post:
      operationId: createAgent
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAgentRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Agent"
          description: Agent principal. No secret.
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Register an agent principal. Not a Hydra secret. Not MCP.
      tags:
      - Owner
  /v1/agents/{name}/revoke:
    post:
      operationId: revokeAgent
      parameters:
      - explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Agent"
          description: Revoked agent. No token or secret.
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: "Revoke an agent. Idempotent. Kills grants, sessions, and in-flight\
        \ Use. Record stays for audit."
      tags:
      - Owner
  /v1/sessions:
    get:
      operationId: listSessions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionsResponse"
          description: Sessions
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Active sandbox sessions. Metadata only. Never the token. Not MCP.
      tags:
      - Owner
    post:
      operationId: createSession
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSessionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSessionResponse"
          description: Session plus token once.
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: "Mint a short-lived Use lease onto an existing agent. Token is in this\
        \ response once. Sandbox gets the session file, not the agent JWT. Default\
        \ 15m, max 1h. Not MCP."
      tags:
      - Owner
  /v1/use:
    post:
      operationId: useItem
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UseRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UseResponse"
          description: "Decision plus upstream result, scrubbed"
        "400":
          description: bad request
        "401":
          description: missing or invalid Bearer
      summary: Call a URL as this agent. The broker injects the credential. The vault
        secret is never in the response.
      tags:
      - Agent
  /v1/requests:
    get:
      operationId: listRequests
      parameters:
      - explode: true
        in: query
        name: status
        required: false
        schema:
          default: open
          enum:
          - open
          - approved
          - denied
          - expired
          - cancelled
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestsResponse"
          description: Requests
        "400":
          description: bad status
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Approval requests filed by level-1 agents. status=open lists only unexpired
        asks. Not MCP.
      tags:
      - Owner
  /v1/requests/stream:
    get:
      operationId: streamRequests
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                type: string
          description: text/event-stream; stays open until the client disconnects
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
      summary: Server-sent events feed of approval-request changes for the owner org.
        Each event is an empty tick — refetch GET /v1/requests for the authoritative
        rows. Not MCP.
      tags:
      - Owner
  /v1/requests/{id}/approve:
    post:
      operationId: approveRequest
      parameters:
      - explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveRequestBody"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalRequest"
          description: The approved request
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
        "404":
          description: no such request in this org
        "409":
          description: already resolved or expired
      summary: Approve an open request — unlocks its level-1 grant for ttl. First
        write wins.
      tags:
      - Owner
  /v1/requests/{id}/deny:
    post:
      operationId: denyRequest
      parameters:
      - explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalRequest"
          description: The denied request
        "401":
          description: missing or invalid Bearer
        "403":
          description: not owner
        "404":
          description: no such request in this org
        "409":
          description: already resolved or expired
      summary: Deny an open request. First write wins; the agent's next use files
        a fresh ask.
      tags:
      - Owner
  /v1/events:
    get:
      operationId: listEvents
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventsResponse"
          description: Recent events for the Bearer agent
        "401":
          description: missing or invalid Bearer
      summary: "This agent's grant events. Decision, item, action. Never secrets.\
        \ Not MCP."
      tags:
      - Agent
  /v1/provision:
    post:
      operationId: provision
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisionResponse"
          description: Provisioned principal
        "401":
          description: missing or invalid human token
      summary: "Signup provisioning. Subject-authenticated — the bearer is a verified\
        \ human ID token, not a member yet. Creates the org, seals the org master\
        \ under the deployment KEK, plants the humans row, and writes the Keto owner/member\
        \ tuples plus the Kratos organization_id stamp. Idempotent; safe to retry.\
        \ Not MCP."
      tags:
      - Human
  /v1/invites:
    post:
      operationId: createInvite
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InviteResponse"
          description: Invite result
        "400":
          description: missing email
        "401":
          description: missing or invalid human token
      summary: Private-alpha invite. Owner-authenticated — the bearer is a verified
        human ID token of a provisioned human. Creates the Kratos identity plus recovery
        link under the inviter's org and emails the setup link via the mail worker.
        recovery_url only appears when mail is not configured (local dev). Idempotent
        for re-invites. Not MCP.
      tags:
      - Human
  /v1/members/{id}:
    delete:
      operationId: removeMember
      parameters:
      - description: Kratos identity id of the member
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/removeMember_200_response"
          description: Member removed
        "401":
          description: missing or invalid human token
        "403":
          description: "caller is a member, not an owner"
        "404":
          description: not a member of this org
      summary: Offboard a member. Owner-only — drops the Keto member tuple and the
        humans row; the removed member's token resolves nothing from that call on.
        Owners cannot be removed this way (demote first). Not MCP.
      tags:
      - Human
  /v1/members/{id}/owner:
    delete:
      operationId: demoteOwner
      parameters:
      - description: Kratos identity id of the owner
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/promoteOwner_200_response"
          description: Owner demoted
        "400":
          description: last-owner demotion refused
        "401":
          description: missing or invalid human token
        "403":
          description: "caller is a member, not an owner"
        "404":
          description: not an owner of this org
      summary: Strip the owners tuple from a co-owner. Owner-only. The last owner
        cannot be demoted — the org would have no administrator. Not MCP.
      tags:
      - Human
    post:
      operationId: promoteOwner
      parameters:
      - description: Kratos identity id of the member
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/promoteOwner_200_response"
          description: Member promoted
        "401":
          description: missing or invalid human token
        "403":
          description: "caller is a member, not an owner"
        "404":
          description: not a member of this org
      summary: Grant an existing member the owners tuple. Owner-only. Not MCP.
      tags:
      - Human
  /v1/me:
    delete:
      operationId: deleteMe
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/deleteMe_200_response"
          description: Account removed from org
        "400":
          description: sole-owner leave refused
        "401":
          description: missing or invalid human token
        "403":
          description: not a provisioned human
      summary: "Leave the org: drops the member (and owner, when present) tuples plus\
        \ the humans row — the token resolves nothing afterward. A sole owner is refused:\
        \ promote a member or delete the org. Not MCP."
      tags:
      - Human
  /v1/org:
    delete:
      operationId: deleteOrg
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/deleteOrg_200_response"
          description: Org purged
        "401":
          description: missing or invalid human token
        "403":
          description: "caller is a member, not an owner"
      summary: "Teardown. Owner-only. Deletes every Keto tuple for the org, then purges\
        \ every vault row (items, grants, sessions, agents, humans, keys) in one transaction.\
        \ Audit rows are kept — teardown must not erase the forensic record. Not MCP."
      tags:
      - Human
  /v1/billing/webhook:
    post:
      operationId: billingWebhook
      parameters:
      - description: "t=<ms>,v1=<hex-hmac>"
        explode: false
        in: header
        name: Vortex-Signature
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: {}
        required: true
      responses:
        "204":
          description: accepted (or durable no-op)
        "400":
          description: malformed event body
        "401":
          description: missing or invalid signature
        "404":
          description: billing webhook not configured
        "413":
          description: body too large
      security: []
      summary: Vortex billing webhook receiver. Auth is the Vortex-Signature header
        (HMAC-SHA256 over the raw body) — no org principal. Subscription and entitlement
        lifecycle events flip the org's plan state; unknown event types are durable
        no-ops. Not MCP.
      tags:
      - Billing
components:
  schemas:
    Item:
      additionalProperties: false
      example:
        id: id
        org_id: org_id
        name: name
        kind: api_key
        owner:
          kind: kind
          id: id
        uris:
        - uris
        - uris
        tags:
        - tags
        - tags
        archived: true
        has_totp: true
        has_file: true
        login: login
      properties:
        id:
          type: string
        org_id:
          type: string
        name:
          type: string
        kind:
          enum:
          - api_key
          - oauth
          - ssh
          - file
          - passkey
          - card
          - identity
          type: string
        owner:
          $ref: "#/components/schemas/Owner"
        uris:
          items:
            type: string
          type: array
          default: null
        tags:
          items:
            type: string
          type: array
          default: null
        archived:
          type: boolean
        has_totp:
          type: boolean
        has_file:
          type: boolean
        login:
          description: Fill username. Metadata. Not a secret. Empty if unset.
          type: string
      required:
      - id
      - kind
      - name
      - org_id
      - owner
      - uris
    Owner:
      additionalProperties: false
      example:
        kind: kind
        id: id
      properties:
        kind:
          type: string
        id:
          type: string
      required:
      - id
      - kind
    ItemsResponse:
      additionalProperties: false
      example:
        items:
        - id: id
          org_id: org_id
          name: name
          kind: api_key
          owner:
            kind: kind
            id: id
          uris:
          - uris
          - uris
          tags:
          - tags
          - tags
          archived: true
          has_totp: true
          has_file: true
          login: login
        - id: id
          org_id: org_id
          name: name
          kind: api_key
          owner:
            kind: kind
            id: id
          uris:
          - uris
          - uris
          tags:
          - tags
          - tags
          archived: true
          has_totp: true
          has_file: true
          login: login
      properties:
        items:
          items:
            $ref: "#/components/schemas/Item"
          type: array
          default: null
      required:
      - items
    UseRequest:
      additionalProperties: false
      example:
        item: item
        url: url
        method: GET
        headers:
          key: headers
        body: body
        body_b64: body_b64
      properties:
        item:
          description: Granted item name
          type: string
        url:
          description: Absolute URL. Host must match the item.
          type: string
        method:
          default: GET
          type: string
        headers:
          additionalProperties:
            type: string
          description: Extra request headers. Never the vault secret.
        body:
          description: Request body. Never the vault secret. Use --body-file on the
            CLI.
          type: string
        body_b64:
          description: Base64 request body for binary payloads. Takes precedence over
            body. Never the vault secret.
          format: byte
          type: string
      required:
      - item
      - url
    UseResponse:
      additionalProperties: false
      example:
        decision: allow
        reason: reason
        approval_id: approval_id
        request_id: request_id
        request_expires_at: 2000-01-23T04:56:07.000+00:00
        status: 0
        headers:
          key:
          - headers
          - headers
        body: body
        body_b64: body_b64
      properties:
        decision:
          enum:
          - allow
          - deny
          - need_approval
          type: string
        reason:
          type: string
        approval_id:
          type: string
        request_id:
          description: "The filed approval request when decision is need_approval.\
            \ Owners resolve it via /v1/requests/{id}."
          type: string
        request_expires_at:
          description: When the filed ask dies unanswered. Agents may keep retrying
            until then.
          format: date-time
          type: string
        status:
          type: integer
        headers:
          additionalProperties:
            items:
              type: string
          description: "Upstream response headers, including Content-Type and Content-Encoding."
        body:
          description: Upstream body with vault secrets scrubbed. Present when the
            body is valid UTF-8.
          type: string
        body_b64:
          description: "Base64 upstream body with vault secrets scrubbed. Present\
            \ when the body is not valid UTF-8 (gzip, images, protobuf)."
          format: byte
          type: string
      required:
      - decision
    AuditEvent:
      additionalProperties: false
      example:
        time: 2000-01-23T04:56:07.000+00:00
        org_id: org_id
        agent_id: agent_id
        item_id: item_id
        action: action
        decision: allow
        reason: reason
        approval_id: approval_id
      properties:
        time:
          format: date-time
          type: string
        org_id:
          type: string
        agent_id:
          type: string
        item_id:
          type: string
        action:
          type: string
        decision:
          enum:
          - allow
          - deny
          - need_approval
          type: string
        reason:
          type: string
        approval_id:
          type: string
      required:
      - action
      - agent_id
      - decision
      - item_id
      - org_id
      - time
    EventsResponse:
      additionalProperties: false
      example:
        events:
        - time: 2000-01-23T04:56:07.000+00:00
          org_id: org_id
          agent_id: agent_id
          item_id: item_id
          action: action
          decision: allow
          reason: reason
          approval_id: approval_id
        - time: 2000-01-23T04:56:07.000+00:00
          org_id: org_id
          agent_id: agent_id
          item_id: item_id
          action: action
          decision: allow
          reason: reason
          approval_id: approval_id
      properties:
        events:
          items:
            $ref: "#/components/schemas/AuditEvent"
          type: array
          default: null
      required:
      - events
    CreateItemRequest:
      additionalProperties: false
      example:
        name: name
        uri: uri
        uris:
        - uris
        - uris
        tags:
        - tags
        - tags
        kind: api_key
        secret: secret
        totp_seed: totp_seed
        login: login
        card:
          number: number
          exp_month: exp_month
          exp_year: exp_year
          cvv: cvv
          holder: holder
        identity:
          given_name: given_name
          family_name: family_name
          address: address
          city: city
          region: region
          postal: postal
          country: country
          phone: phone
          email: email
      properties:
        name:
          type: string
        uri:
          type: string
        uris:
          items:
            type: string
          type: array
          default: null
        tags:
          items:
            type: string
          type: array
          default: null
        kind:
          enum:
          - api_key
          - oauth
          - ssh
          - file
          - passkey
          - card
          - identity
          type: string
        secret:
          description: Vault material. Request only. Never returned.
          type: string
        totp_seed:
          description: TOTP seed. Request only. Never returned.
          type: string
        login:
          description: Fill username. Metadata on the item. Also sealed in the envelope.
            Not a secret.
          type: string
        card:
          $ref: "#/components/schemas/CardFields"
        identity:
          $ref: "#/components/schemas/IdentityFields"
      required:
      - name
    UpdateItemRequest:
      additionalProperties: false
      example:
        uri: uri
        uris:
        - uris
        - uris
        tags:
        - tags
        - tags
        login: login
      properties:
        uri:
          description: Add this autofill host. Does not drop existing hosts.
          type: string
        uris:
          description: Replace autofill hosts with this list.
          items:
            type: string
          type: array
          default: null
        tags:
          items:
            type: string
          type: array
          default: null
        login:
          description: Fill username. Metadata on the item. Also sealed in the envelope.
            Does not rotate the secret.
          type: string
    CreateGrantRequest:
      additionalProperties: false
      example:
        agent: agent
        human: human
        item: item
        level: level1
        expires: expires
      properties:
        agent:
          description: Agent id. XOR human.
          type: string
        human:
          description: Kratos identity id. Same Grant.agent_id. Not email. XOR agent.
          type: string
        item:
          type: string
        level:
          enum:
          - level1
          - level2
          type: string
        expires:
          description: Go duration. Empty is forever.
          type: string
      required:
      - item
      - level
    Grant:
      additionalProperties: false
      example:
        id: id
        org_id: org_id
        agent_id: agent_id
        item_id: item_id
        level: level
        expires_at: 2000-01-23T04:56:07.000+00:00
      properties:
        id:
          type: string
        org_id:
          type: string
        agent_id:
          type: string
        item_id:
          type: string
        level:
          type: string
        expires_at:
          format: date-time
          type: string
      required:
      - agent_id
      - id
      - item_id
      - level
      - org_id
    GrantsResponse:
      additionalProperties: false
      example:
        grants:
        - id: id
          org_id: org_id
          agent_id: agent_id
          item_id: item_id
          level: level
          expires_at: 2000-01-23T04:56:07.000+00:00
        - id: id
          org_id: org_id
          agent_id: agent_id
          item_id: item_id
          level: level
          expires_at: 2000-01-23T04:56:07.000+00:00
      properties:
        grants:
          items:
            $ref: "#/components/schemas/Grant"
          type: array
          default: null
      required:
      - grants
    Agent:
      additionalProperties: false
      example:
        kind: kind
        id: id
        org_id: org_id
        owner:
          kind: kind
          id: id
        revoked_at: 2000-01-23T04:56:07.000+00:00
      properties:
        kind:
          type: string
        id:
          type: string
        org_id:
          type: string
        owner:
          $ref: "#/components/schemas/Owner"
        revoked_at:
          description: Set when the agent is revoked. Record stays for audit.
          format: date-time
          type: string
      required:
      - id
      - kind
      - org_id
    AgentsResponse:
      additionalProperties: false
      example:
        agents:
        - kind: kind
          id: id
          org_id: org_id
          owner:
            kind: kind
            id: id
          revoked_at: 2000-01-23T04:56:07.000+00:00
        - kind: kind
          id: id
          org_id: org_id
          owner:
            kind: kind
            id: id
          revoked_at: 2000-01-23T04:56:07.000+00:00
      properties:
        agents:
          items:
            $ref: "#/components/schemas/Agent"
          type: array
          default: null
      required:
      - agents
    CreateAgentRequest:
      additionalProperties: false
      example:
        name: name
      properties:
        name:
          type: string
      required:
      - name
    Session:
      additionalProperties: false
      description: Sandbox Use lease metadata. Never the token.
      example:
        id: id
        org_id: org_id
        agent_id: agent_id
        expires_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        revoked_at: 2000-01-23T04:56:07.000+00:00
        renewed_at: 2000-01-23T04:56:07.000+00:00
        ttl: 0
        max_ttl: 6
        max_uses: 1
        uses: 5
      properties:
        id:
          type: string
        org_id:
          type: string
        agent_id:
          type: string
        expires_at:
          format: date-time
          type: string
        created_at:
          format: date-time
          type: string
        revoked_at:
          format: date-time
          type: string
          nullable: true
        renewed_at:
          format: date-time
          type: string
          nullable: true
        ttl:
          description: Initial lease duration in seconds.
          type: integer
        max_ttl:
          description: Maximum cumulative lifetime in seconds.
          type: integer
        max_uses:
          description: Maximum successful Use calls. 0 = unlimited.
          type: integer
        uses:
          description: Use calls that passed authorization and reached upstream consumption.
          type: integer
      required:
      - agent_id
      - created_at
      - expires_at
      - id
      - max_ttl
      - max_uses
      - org_id
      - ttl
      - uses
    SessionsResponse:
      additionalProperties: false
      example:
        sessions:
        - id: id
          org_id: org_id
          agent_id: agent_id
          expires_at: 2000-01-23T04:56:07.000+00:00
          created_at: 2000-01-23T04:56:07.000+00:00
          revoked_at: 2000-01-23T04:56:07.000+00:00
          renewed_at: 2000-01-23T04:56:07.000+00:00
          ttl: 0
          max_ttl: 6
          max_uses: 1
          uses: 5
        - id: id
          org_id: org_id
          agent_id: agent_id
          expires_at: 2000-01-23T04:56:07.000+00:00
          created_at: 2000-01-23T04:56:07.000+00:00
          revoked_at: 2000-01-23T04:56:07.000+00:00
          renewed_at: 2000-01-23T04:56:07.000+00:00
          ttl: 0
          max_ttl: 6
          max_uses: 1
          uses: 5
      properties:
        sessions:
          items:
            $ref: "#/components/schemas/Session"
          type: array
          default: null
      required:
      - sessions
    CreateSessionRequest:
      additionalProperties: false
      example:
        agent: agent
        ttl: ttl
        max_uses: 0
      properties:
        agent:
          type: string
        ttl:
          description: Go duration. Default 15m. Max 1h.
          type: string
        max_uses:
          description: Maximum successful Use calls. 0 = unlimited.
          type: integer
      required:
      - agent
    CreateSessionResponse:
      additionalProperties: false
      description: Token is create-only. Never list. Never MCP.
      example:
        id: id
        org_id: org_id
        agent_id: agent_id
        expires_at: 2000-01-23T04:56:07.000+00:00
        created_at: 2000-01-23T04:56:07.000+00:00
        revoked_at: 2000-01-23T04:56:07.000+00:00
        renewed_at: 2000-01-23T04:56:07.000+00:00
        ttl: 0
        max_ttl: 6
        max_uses: 1
        uses: 5
        token: token
      properties:
        id:
          type: string
        org_id:
          type: string
        agent_id:
          type: string
        expires_at:
          format: date-time
          type: string
        created_at:
          format: date-time
          type: string
        revoked_at:
          format: date-time
          type: string
          nullable: true
        renewed_at:
          format: date-time
          type: string
          nullable: true
        ttl:
          type: integer
        max_ttl:
          type: integer
        max_uses:
          type: integer
        uses:
          description: Use calls that passed authorization and reached upstream consumption.
          type: integer
        token:
          type: string
      required:
      - agent_id
      - created_at
      - expires_at
      - id
      - max_ttl
      - max_uses
      - org_id
      - token
      - ttl
      - uses
    CardFields:
      additionalProperties: false
      description: Request only. Never returned. Never MCP.
      example:
        number: number
        exp_month: exp_month
        exp_year: exp_year
        cvv: cvv
        holder: holder
      properties:
        number:
          type: string
        exp_month:
          type: string
        exp_year:
          type: string
        cvv:
          type: string
        holder:
          type: string
    IdentityFields:
      additionalProperties: false
      description: Request only. Never returned. Never MCP.
      example:
        given_name: given_name
        family_name: family_name
        address: address
        city: city
        region: region
        postal: postal
        country: country
        phone: phone
        email: email
      properties:
        given_name:
          type: string
        family_name:
          type: string
        address:
          type: string
        city:
          type: string
        region:
          type: string
        postal:
          type: string
        country:
          type: string
        phone:
          type: string
        email:
          type: string
    ImportResponse:
      additionalProperties: false
      example:
        names:
        - names
        - names
        count: 0
      properties:
        names:
          items:
            type: string
          type: array
          default: null
        count:
          type: integer
      required:
      - count
      - names
    ProvisionResponse:
      additionalProperties: false
      example:
        subject: subject
        org_id: org_id
      properties:
        subject:
          type: string
        org_id:
          type: string
      required:
      - org_id
      - subject
    InviteRequest:
      additionalProperties: false
      example:
        email: email
      properties:
        email:
          format: email
          type: string
      required:
      - email
    InviteResponse:
      additionalProperties: false
      example:
        identity_id: identity_id
        emailed: true
        recovery_url: recovery_url
      properties:
        identity_id:
          type: string
        emailed:
          type: boolean
        recovery_url:
          description: Only present when mail delivery is not configured (local dev).
          type: string
      required:
      - emailed
      - identity_id
    ApprovalRequest:
      additionalProperties: false
      example:
        id: id
        agent_id: agent_id
        item_id: item_id
        grant_id: grant_id
        action: action
        status: open
        created_at: 2000-01-23T04:56:07.000+00:00
        expires_at: 2000-01-23T04:56:07.000+00:00
        resolved_at: 2000-01-23T04:56:07.000+00:00
        resolved_by: resolved_by
        approval_id: approval_id
      properties:
        id:
          type: string
        agent_id:
          type: string
        item_id:
          type: string
        grant_id:
          type: string
        action:
          type: string
        status:
          enum:
          - open
          - approved
          - denied
          - expired
          - cancelled
          type: string
        created_at:
          format: date-time
          type: string
        expires_at:
          format: date-time
          type: string
        resolved_at:
          format: date-time
          type: string
        resolved_by:
          description: The Kratos identity of the owner who answered — first write
            wins.
          type: string
        approval_id:
          description: The grant approval created by an approve resolution.
          type: string
      required:
      - action
      - agent_id
      - created_at
      - expires_at
      - grant_id
      - id
      - item_id
      - status
    RequestsResponse:
      additionalProperties: false
      example:
        requests:
        - id: id
          agent_id: agent_id
          item_id: item_id
          grant_id: grant_id
          action: action
          status: open
          created_at: 2000-01-23T04:56:07.000+00:00
          expires_at: 2000-01-23T04:56:07.000+00:00
          resolved_at: 2000-01-23T04:56:07.000+00:00
          resolved_by: resolved_by
          approval_id: approval_id
        - id: id
          agent_id: agent_id
          item_id: item_id
          grant_id: grant_id
          action: action
          status: open
          created_at: 2000-01-23T04:56:07.000+00:00
          expires_at: 2000-01-23T04:56:07.000+00:00
          resolved_at: 2000-01-23T04:56:07.000+00:00
          resolved_by: resolved_by
          approval_id: approval_id
      properties:
        requests:
          items:
            $ref: "#/components/schemas/ApprovalRequest"
          type: array
          default: null
      required:
      - requests
    ApproveRequestBody:
      additionalProperties: false
      example:
        ttl: ttl
      properties:
        ttl:
          description: Go duration for the grant approval lifetime (default 15m).
          type: string
    removeMember_200_response:
      example:
        removed: true
      properties:
        removed:
          type: boolean
    promoteOwner_200_response:
      example:
        owner: true
      properties:
        owner:
          type: boolean
    deleteMe_200_response:
      example:
        deleted: true
      properties:
        deleted:
          type: boolean
    deleteOrg_200_response:
      example:
        items: 0
        grants: 6
        sessions: 1
        agents: 5
        humans: 5
        keys: 2
      properties:
        items:
          type: integer
        grants:
          type: integer
        sessions:
          type: integer
        agents:
          type: integer
        humans:
          type: integer
        keys:
          type: integer
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: Hydra JWT. Identity is the bound agent or a Keto member human.
        The host is not identity.
      scheme: bearer
      type: http
